skip_list:
  - unnamed-task
  - empty-string-compare
  # Skip the bogus warning: 'Tasks that run when changed should likely be handlers'
  - ANSIBLE0016
  - '503'
  # Skip the bogus warning about rsync.
  - '303'
  # TODO: Convert to explicit modes
  - risky-file-permissions
  - fqcn-builtins
