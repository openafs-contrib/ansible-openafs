# {{ ansible_managed }}

[libdefaults]
    default_realm = {{ afs_realm }}

[realms]
    {{ afs_realm }} = {
        kdc = {{ ansible_hostname }}
        admin_server = {{ ansible_hostname }}
    }

[domain_realm]

[login]
