---
dependency:
  name: galaxy
driver:
  name: vagrant
  provider:
    name: libvirt
platforms:
  - name: instance-krbserver
    box: ${BOX-generic/centos8}
    memory: 1024
    cpus: 1
    groups:
      - afs_kdcs
provisioner:
  name: ansible
verifier:
  name: ansible
