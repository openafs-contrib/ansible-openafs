---
- name: Ensure CellServDB.dist exists
  copy:
    content: ""
    dest: "{{ viceetcdir }}/CellServDB.dist"
    mode: 0644
    force: no

- name: Update cell configuration
  template:
    src: "{{ item }}.j2"
    dest: "{{ viceetcdir }}/{{ item }}"
    mode: 0644
  with_items:
    - CellServDB.local
    - ThisCell
    - cacheinfo
