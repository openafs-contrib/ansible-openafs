---
- name: Add OpenAFS yum repo
  yum_repository:
    name: openafs
    description: OpenAFS yum repo
    baseurl: "{{ openafs_server_repourl }}"
    gpgcheck: no

- name: Install OpenAFS server packages
  yum:
    state: present
    name:
      - openafs-server
      - openafs-krb5
